{
  "name": "matrix-analytics-events",
  "version": "0.0.1",
  "description": "JSON schema for analytics events from matrix SDKs",
  "author": "Element",
  "license": "Apache-2.0",
  "devDependencies": {
    "json-schema-to-typescript": "^10.1.5",
    "quicktype": "^15.0.260"
  },
  "scripts": {
    "build": "yarn build:ts && yarn build:kt && yarn build:swift",
    "build:ts": "json2ts schemas/ -o types/typescript/",
    "build:kt": "scripts/build-kotlin.sh",
    "build:swift": "scripts/build-swift.sh"
  }
}
