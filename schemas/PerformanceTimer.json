{
  "type": "object",
  "description": "Triggered after timing a long running operation in the app.",
  "properties": {
    "eventName": {
      "enum": ["PerformanceTimer"]
    },
    "name": {
      "description": "The timer that is being reported.",
      "type": "string",
      "enum": [
        "StartupInitialSync",
        "StartupIncrementalSync",
        "StartupStorePreload",
        "StartupStoreReady",
        "StartupLaunchScreen",
        "InitialSyncRequest",
        "InitialSyncParsing"
      ]
    },
    "timeMs": {
      "description": "The time reported by the timer in milliseconds.",
      "type": "integer"
    },
    "context": {
      "description": "Client defined, can be used for debugging.",
      "type": "string"
    }
  },
  "required": ["name", "timeMs", "eventName"],
  "additionalProperties": false
}
