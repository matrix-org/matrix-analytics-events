{
  "type": "object",
  "description": "Triggered when an error occurred in a call.",
  "properties": {
    "eventName": {
      "enum": ["CallError"]
    },
    "placed": {
      "description": "Whether this user placed it.",
      "type": "boolean"
    },
    "isVideo": {
      "description": "Whether its a video call or not.",
      "type": "boolean"
    },
    "numParticipants": {
      "description": "Number of participants in the call.",
      "type": "integer"
    }
  },
  "required": ["placed", "isVideo", "numParticipants", "eventName"],
  "additionalProperties": false
}
