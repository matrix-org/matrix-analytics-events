{
  "type": "object",
  "description": "Triggered when a poll vote has been cast.",
  "properties": {
    "eventName": {
      "enum": ["PollVote"]
    },
    "doNotUse": {
      "description": "Do not use this. Remove this property when the kotlin type generator can properly generate types without proprties other than the event name.",
      "type": "boolean"
    }
  },
  "required": ["eventName"],
  "additionalProperties": false
}
