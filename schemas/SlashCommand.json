{
  "type": "object",
  "description": "Triggered when the user runs a slash command in their composer.",
  "properties": {
    "eventName": {
      "enum": ["SlashCommand"]
    },
    "command": {
      "description": "The the slash command text. e.g. /me ",
      "type": "string"
    },
    "editor": {
      "description": "Whether this message was composed in legacy editor, the new the rich text editor or the new plain text editor",
      "type": "string",
      "enum": [
        "Legacy",
        "RteFormatting",
        "RtePlain"
      ]
    }
  },
  "required": ["command", "eventName", "editor"],
  "additionalProperties": false
}