{
  "type": "object",
  "description": "Triggered when the user runs a slash command in their composer.",
  "properties": {
    "eventName": {
      "enum": ["SlashCommand"]
    },
    "command": {
      "description": "The name of this command.",
      "type": "string",
      "enum": [
        "Part",
        "Invite"
      ]
    },
    "isRichTextEditor": {
      "description": "Whether this message was composed in the rich text editor (as opposed to the predating markdown-based editor).",
      "type": "boolean"
    }
  },
  "required": ["command", "eventName", "isRichTextEditor"],
  "additionalProperties": false
}
